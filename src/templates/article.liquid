{% include "_header.liquid" %}
<article class="post">
    <header>
    <h1>{{ model.title }}</h1>
    <span class="date">{{ model.date }}</span>
    </header>

    <section class="content">
    {{ model.content }}
    </section>
</article>
{% include "_footer.liquid" %}